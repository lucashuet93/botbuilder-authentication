/**
 * Defines available authentication providers.
 * @enum {string}
 */
export enum ProviderType {
	AzureADv2 = 'azureADv2',
	Facebook = 'facebook',
	Google = 'google',
	Github = 'github'
}

/**
 * Defines available authentication providers.
 * @enum {string}
 */
export enum ServerType {
	Express = 'express',
	Restify = 'restify'
}